#include <string>
#include<list>
#include <vector>

using namespace std;

vector<int> solution(long long n) {
    list<int> a;
    for (int i = 1; i < a.size(); ++i) 
        a.push_back(i);

    a.reverse();
    for (auto i : a)
    {
        cout<<
    }

    
}
